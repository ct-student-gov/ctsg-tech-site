{
  "name": "ctsg-tech-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wordpress:test-snippet": "node scripts/build-wordpress-test.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check public/wordpress-bridge.js && node --check scripts/dev.mjs"
  },
  "engines": { "node": ">=20" }
}
